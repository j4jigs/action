name: Simple Workflow

on:
  push:
    branches:
      - main

jobs:
  simple-job:
    runs-on: ubuntu-latest
    steps:
       - name: Print Message
        run: echo "Hello, GitHub Actions!"
